import { Reducers, State } from 'reactn/default';
import ReactNProvider from 'reactn/types/provider';
import { compose } from 'redux';
declare type BooleanFunction = () => boolean;
interface EnhancerOptions {
    name: string;
}
export interface Window {
    __REDUX_DEVTOOLS_EXTENSION__?: any;
    __REDUX_DEVTOOLS_EXTENSION_COMPOSE__: (options: EnhancerOptions) => typeof compose;
}
export default function addReactNDevTools<G extends {} = State, R extends {} = Reducers>(Provider?: ReactNProvider<G, R>): BooleanFunction;
export {};
